desc "This tasks calls methods on TweetDonationService to store new tweets and create donations"
task :store_tweets_and_create_donations => :environment do
  puts "Updating feed..."
  puts "done."
end
